#include<stdio.h>
#include<string.h>
int main() {
    char ch[200];
    char str1[300];
    fgets(ch,sizeof(ch),stdin);
    int len=strlen(ch);
    int count=0;
    for (int i=0;i<len;i++) {
       str1[i]=ch[i];
    }
    
    printf("%s",str1);

}
