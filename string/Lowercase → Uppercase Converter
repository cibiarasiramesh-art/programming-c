#include<stdio.h>
#include<string.h>
int main() {
    char ch[200];
    fgets(ch,sizeof(ch),stdin);
    int len=strlen(ch);
    for (int i=0;i<len;i++) {
        int val=ch[i]-32;
        printf("%c",val);
    }
}
