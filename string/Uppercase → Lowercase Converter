#include<stdio.h>
#include<string.h>
#include<ctype.h>
int main() {
    char ch[200];
    fgets(ch,sizeof(ch),stdin);
    int len=strlen(ch);
    for (int i=0;i<len;i++) {
        printf("%c",tolower(ch[i]));
    }

}
