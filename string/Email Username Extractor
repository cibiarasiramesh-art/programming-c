#include<stdio.h>
#include<string.h>
int main() {
    char ch[200];
    scanf("%s",ch);
    for (int i=0;ch[i]!='\0';i++) {
        if (ch[i]!='@') {
            printf("%c",ch[i]);
        }
        else {
            break;
        }
    }

}
