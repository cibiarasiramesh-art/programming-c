#include<stdio.h>
int main() {
    int n;
    scanf("%d",&n);

    int add=0;
    for (int i=1;i<=n;i++) {
        int t;
        scanf("%d",&t);
        add=add+t;
    }
    printf("total charge: %d ",add);

}
