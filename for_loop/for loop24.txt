#include<stdio.h>
#include<math.h>
int main() {
    int n;
    scanf("%d",&n);
    int o=n;
    int count=0;
    int add=0;
    for (int i=n;n!=0;n=n/10) {
        int div=n%10;
        count++;
    }
    for (int j=0;j<count;j++) {
        for (int l=0;l<1;l++){
            int rem=o%10;
            int power=(int)pow(2,j);
            int res=rem*power;
            add=add+res;
            o=o/10;
        }
    }printf("%d",add);
    return 0;

}