#include<stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int add=0;
    for (int i=1;i<n;i++) {
        if (n%i==0) {
            add=add+i;
        }
    }
    if (add==n) {
        printf("yes");
    }
    else {
        printf("no");
    }
    return 0;
}
