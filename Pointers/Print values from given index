#include<stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int arr[n];
    int *p=arr;
    for (int i=0;i<n;i++) {
        scanf("%d",&arr[i]);
    }
    int start;
    scanf("%d",&start);
    for (int i=start;i<n;i++) {
        printf("%d ",*p+i);
    }
}
