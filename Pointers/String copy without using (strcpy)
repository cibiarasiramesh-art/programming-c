#include<stdio.h>
#include<string.h>
int main() {
    char str1[100],str2[100];
    fgets(str1,sizeof(str1),stdin);
    int i=0;
    int len=strlen(str1);
    char *p=str1;
    char *q=str2;
    while (i<len) {
        *(q+i)=*(p+i);
        i++;
    }
    *(q+i)='\0';
    printf("%s",q);
}
