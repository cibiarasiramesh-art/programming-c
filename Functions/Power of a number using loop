#include<stdio.h>
void power(int n,int p) {
    int mul=1;
    for (int i=1;i<=p;i++) {
        mul=mul*n;
    }
    printf("%d",mul);
}
int main() {
    int n,p;
    scanf("%d %d",&n,&p);
    power(n,p);
}
