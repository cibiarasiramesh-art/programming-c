#include<stdio.h>
void count(int n) {
    int count=0;
    while (n!=0) {
        int rem=n%10;
        n=n/10;
        count++;
    }
    printf("%d",count);

}
int main() {
    int n;
    scanf("%d",&n);
    count(n);
}
