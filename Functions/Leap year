#include<stdio.h>
void leap(int y) {
    if (y%400==0 || (y%4==0 && y%100!=0)) {
        printf("leap year");
    }
    else {
        printf("not a leap year");
    }
}
int main() {
    int y;
    scanf("%d",&y);
    leap(y);
}
