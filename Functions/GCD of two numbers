#include<stdio.h>
void gcd(int n,int m) {
    int gcd;
    int small=(n<m)?n:m;
    for (int i=1;i<=small;i++) {
        if (n%i==0 && m%i==0) {
            gcd=i;
        }
    }
    printf("%d",gcd);
}
int main() {
    int n,m;
    scanf("%d %d",&n,&m);
    gcd(n,m);
}
