#include <stdio.h>

int main() {
    int class, age;
    int fare;

    scanf("%d", &class);
    scanf("%d", &age);
    int fare1=0;
    switch (class) {
        case 1:  
            fare = 300;
            if (age < 12) {
                fare1 = fare / 2;              
            }
            else if (age >= 60) {
                fare1 = (33/100)*fare; 
            }
            break;

        case 2:  
            
            printf("%d",1000);
        default:
            printf("Invalid class");
            
    }

    printf("Fare ₹%d", fare1);
    return 0;
}
