a#include <stdio.h>

int main() {
    int n;

    scanf("%d", &n);

    int arr[n];

    for(int i = 0; i < n; i++)

        scanf("%d", &arr[i]);

    int index;

    scanf("%d", &index);

    printf("%d ", arr[index]);

    for(int i = index + 1; i < n; i++)

        printf("%d ", arr[i]);

    for(int i = 0; i < index; i++)

        printf("%d ", arr[i]);

    return 0;

}
